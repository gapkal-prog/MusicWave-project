{
  "$schema_note": "Single source of truth for MusicWave package versions and compatibility ranges. Update in the same change that bumps any package version or the data schema (PROJECT_PLAN.md, Stage 0).",
  "product": "MusicWave",
  "manifest_version": "1.0.0",
  "updated": "2026-09-06",
  "license": "GPL-2.0-or-later",
  "packages": {
    "music-wave-core": {
      "version": "0.11.3",
      "type": "wordpress-plugin",
      "schema_version": "0.11.0",
      "requires": {
        "wordpress": ">=6.6",
        "php": ">=7.4"
      },
      "tested_up_to": {
        "wordpress": "6.8"
      },
      "optional_dependencies": {
        "woocommerce": ">=9.0 (commerce features only)"
      }
    },
    "music-wave-vip": {
      "version": "0.5.0",
      "type": "wordpress-plugin",
      "requires": {
        "wordpress": ">=6.6",
        "php": ">=7.4",
        "music-wave-core": ">=0.9.0"
      },
      "tested_up_to": {
        "wordpress": "6.8"
      },
      "optional_dependencies": {
        "woocommerce": ">=9.0 (VIP plan products; role/filter adapters are standalone)"
      }
    },
    "musicwave": {
      "version": "0.8.0",
      "type": "wordpress-theme",
      "requires": {
        "wordpress": ">=6.6",
        "php": ">=7.4"
      },
      "tested_up_to": {
        "wordpress": "6.8"
      },
      "recommended_dependencies": {
        "music-wave-core": ">=0.9.0"
      }
    }
  },
  "support_policy": "docs/adr/0003-runtime-support-policy.md",
  "notes": [
    "Schema version is owned by music-wave-core (MigrationRunner::LATEST_VERSION) and must match this manifest.",
    "Compatibility claims listed here must be backed by recorded CI or manual test evidence before release (PROJECT_PLAN.md Stage 7)."
  ]
}
